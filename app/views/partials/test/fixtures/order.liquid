{% liquid
  assign status = status | default: 'app.statuses.orders.placed'

  function order = 'lib/commands/orders/create', current_profile: current_profile, item: item
  function status = 'lib/commands/statuses/create', profile_id: current_profile.id, object_id: order.id, name: status
  function order = 'lib/queries/orders/find', id: order.id

  return order
%}
