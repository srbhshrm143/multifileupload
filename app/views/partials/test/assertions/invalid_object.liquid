{% liquid
  hash_assign contract['total'] = contract['total'] | plus: 1
  if object.valid
    function contract = 'test/register_error_f', contract: contract, field_name: field_name, message: object.errors
  endif
  return contract
%}
