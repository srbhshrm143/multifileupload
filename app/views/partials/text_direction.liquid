  {% liquid
  assign languages = 'ar,arc,dv,fa,ha,he,khw,ks,ku,ps,ur,yi'  | split: ','
    if languages contains lang
      assign direction = 'rtl'
    else
      assign direction = 'ltr'
    endif

    return direction
%}
