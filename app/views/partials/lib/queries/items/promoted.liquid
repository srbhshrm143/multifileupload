{% liquid
  assign statuses = 'published,unavailable' | split: ','
  function items = 'lib/queries/items/search', promoted: true, limit: limit, statuses: statuses, lang: context.language, main_variant: true

  return items
%}
