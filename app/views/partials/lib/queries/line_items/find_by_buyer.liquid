{% liquid
  if id == blank or buyer_id == blank
    return null
  endif

  assign lang = lang | default: context.language
  graphql r  = 'line_items/find', id: id, buyer: buyer_id, lang: lang

  return r.line_items.results.first
%}
