{% liquid
  assign page = page | to_positive_integer: 1

  graphql r = 'organizations/search', from: from, to: to, limit: limit, slugs: slugs, name: name, page: page
  include 'lib/handle_graphql_response', r: r, name: 'queries/organizations/search'

  return r.records
%}
