{% liquid
  if id == blank or seller_id == blank
    return null
  endif

  assign lang = lang | default: context.language

  graphql r  = 'line_item_returns/search', id: id, limit: 1, seller_id: seller_id, lang: lang

  return r.line_item_returns.results.first
%}
