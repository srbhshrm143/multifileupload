{% liquid
  if id == blank
    log '(users) ID cannot be blank when searching for audience'
    return null
  endif

  graphql r  = 'activities/user', id: id

  assign user = r.users.results.first

  hash_assign user['url'] = "/profile/" | append: user.slug

  return user
%}
