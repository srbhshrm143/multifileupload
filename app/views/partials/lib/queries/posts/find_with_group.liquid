{% liquid
  if id == blank
    log 'Something went wrong. ID cannot be blank.', type: 'ERROR'
    return null
  endif

  graphql r  = 'posts/search_with_group', id: id, limit: 1

  return r.posts.results.first
%}
