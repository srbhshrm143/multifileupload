{% liquid
       assign lang = lang | default: context.language
       if keyword != blank
         assign keyword_res = keyword | downcase
       endif
       graphql r = 'quick_search/search', keyword: keyword_res, lang: lang, limit: limit
       include 'lib/handle_graphql_response', r: r, name: 'queries/quick_search/search'
     
       return r
     %}
