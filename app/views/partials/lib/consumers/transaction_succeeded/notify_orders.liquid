{% liquid
  function transaction = 'lib/queries/transactions/find', id: event.target.id, with_order_details: true
  function res = 'lib/commands/orders/mark_paid', orders: transaction.orders, profile_id: transaction.profile_id
%}
