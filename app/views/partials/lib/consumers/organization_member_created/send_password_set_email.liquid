{% liquid
  function profile = 'lib/queries/profiles/find', id: event.target.id
  function organization = 'lib/queries/organizations/find', id: event.object.id

  function object = 'lib/commands/authentication_links/create', email: profile.user.email, host: context.location.host
  hash_assign object['organization'] = organization
  function email = 'lib/commands/emails/organizations_member_set_password', object: object
%}
