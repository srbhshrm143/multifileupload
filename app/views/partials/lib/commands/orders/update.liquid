{% liquid
  function object = 'lib/commands/orders/update/build', object: object, order: order
  function object = 'lib/commands/orders/update/check', object: object, order: order

  if object.valid
    function object = 'lib/commands/execute', object: object, mutation_name: 'orders/update'
  endif

  return object
%}
