{% liquid
  function object = 'lib/commands/orders/venues/build', object: object, venue: venue, vendor: vendor, current_profile: current_profile, params: params
  function object = 'lib/commands/orders/venues/check', object: object

  function object = 'lib/commands/execute', object: object, mutation_name: 'orders/new'
  if object.valid
  endif

  return object
%}