{% liquid
  function object = 'lib/commands/users/update/build', object: object, current_user: current_user
  function object = 'lib/commands/users/update/check', object: object, current_user: current_user, c: c

  if object.valid
    function object = 'lib/commands/execute', object: object, mutation_name: 'users/update', selection: 'user_update'

    if object.valid
      function _event = 'lib/commands/events/create', type: 'account_updated', actor_id: object.id
    endif
  endif

  return object
%}
