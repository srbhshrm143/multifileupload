{% liquid
  function object = 'lib/commands/users/reset_login_attempts/build', object: object, default_login_attempts: default_login_attempts, user_id: user_id
  function object = 'lib/commands/users/reset_login_attempts/check', object: object

  if object.valid
    function object = 'lib/commands/execute', object: object, mutation_name: 'users/update', selection: 'user_update'
  endif

  return object
%}
