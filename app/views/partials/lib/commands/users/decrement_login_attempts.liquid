{% liquid
  function object = 'lib/commands/users/decrement_login_attempts/build', object: object, default_login_attempts: default_login_attempts
  function object = 'lib/commands/users/decrement_login_attempts/check', object: object

  if object.valid
    function object = 'lib/commands/execute', object: object, mutation_name: 'users/update', selection: 'user_update'
  endif

  return object
%}
