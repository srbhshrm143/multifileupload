{% parse_json object %}
  {
    "promo_code_id": {{ promo_code.id| json }},
    "lang": {{ lang | default: "en" | json }},
    "description": {{ object.description | json }}
  }
{% endparse_json %}

{% liquid
  return object
%}
