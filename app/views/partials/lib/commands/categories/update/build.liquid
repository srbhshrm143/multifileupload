{% parse_json data %}
  {
    "id": {{ category.id | json }},
    "key": {{ category.key | json }}
  }
{% endparse_json %}


{% liquid
  hash_assign data['category_details'] = object.category_details

  return data
%}
