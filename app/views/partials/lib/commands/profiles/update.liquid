{% liquid
  function object = 'lib/commands/profiles/update/build', object: object, profile: profile
  function object = 'lib/commands/profiles/update/check', object: object, current_profile: profile

  if object.valid
    function object = 'lib/commands/execute', object: object, mutation_name: 'profiles/update'

    if object.valid
      function _event = 'lib/commands/events/create', type: 'profile_updated', actor_id: object.id
    endif
  endif

  return object
%}
