{% liquid
  function object = 'lib/commands/project_offers/delete/check',  object: object

  if object.valid
    function object = 'lib/commands/execute', mutation_name: 'project_offers/delete', object: object
  endif

  return object
%}
