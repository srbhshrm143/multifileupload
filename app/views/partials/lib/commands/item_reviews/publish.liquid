{%  liquid
  function object = 'lib/commands/item_reviews/publish/build', object: object
  function object = 'lib/commands/item_reviews/publish/check', object: object

  if object.valid
    function object = 'lib/commands/execute', mutation_name: 'item_reviews/publish' object: object
  endif

  return object
%}
