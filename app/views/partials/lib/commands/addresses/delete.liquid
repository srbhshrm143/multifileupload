{% liquid
  function object = 'lib/commands/addresses/delete/check',  object: object

  if object.valid
    function object = 'lib/commands/execute', mutation_name: 'addresses/delete', object: object
  endif

  return object
%}
