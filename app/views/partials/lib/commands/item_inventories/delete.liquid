{% liquid
  function object = 'lib/commands/item_inventories/delete/check',  object: object

  if object.valid
    function object = 'lib/commands/execute', mutation_name: 'item_inventories/delete', object: object
  endif

  return object
%}
