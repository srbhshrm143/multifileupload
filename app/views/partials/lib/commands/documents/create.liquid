{% liquid
  function object = 'lib/commands/documents/create/build', object: object, current_profile: current_profile
  function object = 'lib/commands/documents/create/check', object: object

  if object.valid
    function object = 'lib/commands/execute', mutation_name: 'documents/create' object: object
  endif

  return object
%}
