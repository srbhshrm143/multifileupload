{% parse_json object %}
  {
    "direct_url":  {{ object.direct_url | json }},
    "creator_id":     {{ current_profile.id | json }},
    "document_type":     {{ object.document_type | json }},
    "object_uuid":   {{ object.object_uuid | json }}
  }
{% endparse_json %}

{% liquid
  return object
%}
