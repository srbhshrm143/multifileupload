{% liquid
  function object = 'lib/commands/vendor/venue_menus/build', object: object, profile: profile
  function object = 'lib/commands/vendor/venue_menus/check', object: object

  if object.valid
    function object = 'lib/commands/execute', object: object, mutation_name: 'venue/update/update_venue_menu', selection: 'record_update'
    return object 
  else
    return object
  endif
%}
