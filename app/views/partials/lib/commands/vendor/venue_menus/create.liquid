{% liquid
  function object = 'lib/commands/vendor/venue_menus/build', venue: venue, object: object, profile: profile
  function object = 'lib/commands/vendor/venue_menus/check', object: object

  if object.valid
    function object = 'lib/commands/execute', object: object, mutation_name: 'venue/venue_menus', selection: 'model_create'
    return object 
  else
    return object
  endif
%}