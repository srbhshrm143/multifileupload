{% liquid
  function object = 'lib/commands/vendor/venue_hours/build', object: object, profile: profile
  function object = 'lib/commands/vendor/venue_hours/check', object: object

  if object.valid
    function object = 'lib/commands/execute', object: object, mutation_name: 'venue/venue_hours', selection: 'model_create'
    return object 
  else
    return object
  endif
%}