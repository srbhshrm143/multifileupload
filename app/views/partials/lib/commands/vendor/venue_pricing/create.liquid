{% liquid
  function object = 'lib/commands/vendor/venue_pricing/build', object: object, profile: profile
  function object = 'lib/commands/vendor/venue_pricing/check', object: object

  if object.valid
    function object = 'lib/commands/execute', object: object, mutation_name: 'venue/venue_pricing', selection: 'model_create'
    return object 
  else
    return object
  endif
%}