{% liquid
  function object = 'lib/commands/vendor/deposits_payments/update/build', object: object, profile: profile
  function object = 'lib/commands/vendor/deposits_payments/update/check', object: object

  if object.valid
    function object = 'lib/commands/execute', object: object, mutation_name: 'venue/update/update_venue_payment', selection: 'record_update'
    return object 
  else
    return object
  endif
%}
