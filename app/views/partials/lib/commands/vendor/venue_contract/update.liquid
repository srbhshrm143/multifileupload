{% liquid
  function object = 'lib/commands/vendor/venue_contract/update/build', object: object, profile: profile
  function object = 'lib/commands/vendor/venue_contract/update/check', object: object

  if object.valid
    function object = 'lib/commands/execute', object: object, mutation_name: 'venue/update/update_venue_contract', selection: 'record_update'
    return object 
  else
    return object
  endif
%}
