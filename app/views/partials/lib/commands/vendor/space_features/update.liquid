{% liquid
  function object = 'lib/commands/vendor/space_features/update/build', object: object, profile: profile
  function object = 'lib/commands/vendor/space_features/update/check', object: object

  if object.valid
    function object = 'lib/commands/execute', object: object, mutation_name: 'venue/update/update_space_features', selection: 'record_update'
    return object 
  else
    return object
  endif
%}
