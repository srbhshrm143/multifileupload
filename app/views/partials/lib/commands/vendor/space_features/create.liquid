{% liquid
  function object = 'lib/commands/vendor/space_features/build',venue_id:venue_id,menu_id:menu_id, object: object,  profile: profile
  function object = 'lib/commands/vendor/space_features/check', object: object

  if object.valid
    function object = 'lib/commands/execute', object: object, mutation_name: 'venue/special_features', selection: 'model_create'
    return object 
  else
    return object
  endif
%}
