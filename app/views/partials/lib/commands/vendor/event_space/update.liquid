{% liquid
  function object = 'lib/commands/vendor/event_space/update/build', object: object, profile: profile
  function object = 'lib/commands/vendor/event_space/update/check', object: object

  if object.valid
    function object = 'lib/commands/execute', object: object, mutation_name: 'venue/update/update_menu_spaces', selection: 'record_update'
    return object 
  else
    return object
  endif
%}
