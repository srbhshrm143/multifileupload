{% liquid
  function object = 'lib/commands/vendor/event_space/build', object: object, profile: profile
  function object = 'lib/commands/vendor/event_space/check', object: object

  if object.valid
    function object = 'lib/commands/execute', object: object, mutation_name: 'venue/venue_event_space/create', selection: 'model_create'
    return object 
  else
    return object
  endif
%}
