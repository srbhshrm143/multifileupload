{% liquid
  function object = 'lib/commands/vendor/advance_notice/build', object: object, profile: profile
  function object = 'lib/commands/vendor/advance_notice/check', object: object

  if object.valid
    function object = 'lib/commands/execute', object: object, mutation_name: 'venue/venue_advance_notice', selection: 'model_create'
    return object 
  else
    return object
  endif
%}