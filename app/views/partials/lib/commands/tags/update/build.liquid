{% parse_json object %}
  {
    "id": {{ object.id | json }},
    "name": {{ object.name | json }},
    "disabled_at": {{ object.disabled_at | json }}
  }
{% endparse_json %}

{% liquid
  return object
%}
