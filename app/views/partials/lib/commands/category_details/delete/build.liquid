{% parse_json object %}
  {
    "id": {{ object.id | json }},
    "lang": {{ object.lang | json }}
  }
{% endparse_json %}

{% liquid
  hash_assign object['translation_key'] = 'categories.' | append: category.key
  return object
%}
