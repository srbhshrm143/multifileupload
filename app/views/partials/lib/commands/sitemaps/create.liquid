{% liquid

  function object = 'lib/commands/sitemaps/create/build'
  function object = 'lib/commands/execute', mutation_name: 'sitemaps/create', object: object, selection: 'record'

  return object
%}
