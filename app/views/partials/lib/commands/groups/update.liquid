{%  liquid
  function object = 'lib/commands/groups/update/build', object: object
  function object = 'lib/commands/groups/update/check', object: object

  if object.valid
    function object = 'lib/commands/execute', object: object, mutation_name: 'groups/update'
  endif

  return object
%}
