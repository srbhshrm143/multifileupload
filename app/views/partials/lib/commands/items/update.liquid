{%  liquid
  function object = 'lib/commands/items/update/build', object: object
  function object = 'lib/commands/items/update/check', object: object

  if object.valid
    function object = 'lib/commands/execute', mutation_name: 'items/update', object: object
  endif

  return object
%}
