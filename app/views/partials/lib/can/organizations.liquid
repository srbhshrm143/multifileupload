{% liquid
  assign can = false
  if organization == blank
    return false
  endif

  assign relation = actor.organizations | detect: r_id: organization.id
  if relation
    assign owner = true
  else
    assign owner = false
  endif

  case do
  when 'edit'
    return owner
  endcase

  return can
%}
