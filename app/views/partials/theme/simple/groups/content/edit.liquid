{% render 'theme/simple/groups/header', group: group, current_profile: current_profile, context: context %}
<div class="container flex flex-col md:flex-row">
  <div class="py-4 px-5 mb-5 shadow-sm bg-panel lg:w-1/5 md:w-1/3 md:mb-0">
    {% render 'theme/simple/groups/nav', tab: tab, group: group, current_profile: current_profile %}
  </div>
  <div class="flex-1 md:ms-10 px-3 py-5">
    <section class="text-gray-900 body-font">
      <div class="grid grid-cols-1 my-6 md:px-5 md:py-5 container">
        {% render 'theme/simple/groups/content/form', object: object, presigned_url: presigned_url, context: context, group: group %}
      </div>
    </section>
  </div>
</div>
