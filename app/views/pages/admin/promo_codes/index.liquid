---
layout: admin
---
{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id
  include 'lib/profile_present_or_redirect', profile: current_profile, allow_uncompleted: true
  include 'lib/profile_must_be', profile: current_profile, action: 'be_admin'

  function promo_codes = 'lib/queries/promo_codes/search', limit: 100, page: 1, lang: context.language, with_organization: true

  render 'theme/simple/admin/promo_codes/index', promo_codes: promo_codes, context: context, current_profile: current_profile
%}
