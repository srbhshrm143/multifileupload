---
slug: admin/conversations/:id
layout: admin
---
{% liquid

  function current_profile = 'lib/current_profile', user_id: context.current_user.id
  include 'lib/profile_present_or_redirect', profile: current_profile, allow_uncompleted: true
  include 'lib/profile_must_be', profile: current_profile, action: 'be_admin'

  function conversation = 'modules/chat/lib/queries/conversations/find', id: context.params.id, include_messages: true

  include 'theme/simple/admin/conversations/show', conversation: conversation
%}
