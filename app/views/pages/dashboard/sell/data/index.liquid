{% liquid
  function current_profile = 'lib/current_organization_profile', user_id: context.current_user.id
  include 'lib/profile_organization_present_or_redirect', profile: current_profile

  function operations = 'lib/queries/bulk_operations/search', limit: 10, profile_id: current_profile.id

  function _ = 'lib/extend_context', document_presign: true
  render 'theme/simple/dashboard/sell/data/index', context: context, operations: operations
%}
