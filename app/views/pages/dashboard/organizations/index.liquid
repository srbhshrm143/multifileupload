{% liquid
  include 'lib/profile_must_be', profile: current_profile, action: 'be_admin'

  function current_profile = 'lib/current_profile', user_id: context.current_user.id, followships: true, membership: true
  include 'lib/profile_present_or_redirect', profile: current_profile

  echo current_profile.joined_group_ids
%}
