---
  slug: guest_terms
---

{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id
  assign object = '{}' | parse_json
  
  
  render 'theme/simple/cust/guest_terms', context: context
%}