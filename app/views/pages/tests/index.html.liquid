---
layout: test
---
{% liquid
  if context.environment == 'staging' or context.environment == 'development'
    graphql tests = 'tests/test_files/search', path: context.params.name | dig: "admin_liquid_partials" | dig: "results"

    render 'test/index', tests: tests
  endif
%}
