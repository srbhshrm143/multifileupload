---
slug: offers/manage
---
{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id
  include 'lib/profile_present_or_redirect', profile: current_profile

  function filters = 'lib/queries/projects/filters', params: context.params
  function project_offers = 'lib/queries/project_offers/search', limit: 20, page: filters.page, keyword: filters.keyword, creator_id: current_profile.id

  render 'theme/simple/project_offers/manage', project_offers: project_offers, page: page, filters: filters, current_profile: current_profile, context: context
%}
