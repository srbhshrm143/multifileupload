{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id
  include 'lib/profile_present_or_redirect', profile: current_profile

  function categories = 'lib/queries/categories/search'

  function _ = 'lib/extend_context', group: group, tags: true, post_templates: true, group_templates: true, photo_presign: true
  render 'theme/simple/groups/new', presigned_url: presigned_url, current_profile: current_profile, context: context, categories: categories.results
%}
