---
  slug: venue/venue_menus/create
  method: post
---

{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id
  assign menus = context.params.menus | json
  function object = 'lib/commands/vendor/venue_menus/create', venue: context.params.venue_menus, object: menus, profile: current_profile.user
  assign venue_id = object.properties.venue_id
  
  include 'redirect_to', event_menus: context.params.venue_menus, venue_id: venue_id, notice: 'venue menus created'
  
  comment

  include 'redirect_to', event_menus: context.params.venue_menus, notice: 'venue menus created'
  endcomment
%}

{{ context.params }}
<br><br>
{{ object }}
<br><br>

