---
  slug: venue/venue_payments/create
  method: post
---

{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id
  
  function object = 'lib/commands/vendor/deposits_payments/create', object: context.params.venue_payments, profile: current_profile.user
  assign venue_id = object.properties.venue_id

  include 'redirect_to', venue_payments: context.params.venue_payments, venue_id: venue_id, notice: 'venue payments created'

  comment
    include 'redirect_to', venue_payments: context.params.venue_payments, notice: 'venue payments created'
  endcomment
%}

{{ context.params }}
<br><br>
{{ object }}
<br><br>

