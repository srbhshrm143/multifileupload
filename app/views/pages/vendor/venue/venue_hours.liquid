---
  slug: venue/venue_hours/create
  method: post
---

{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id
  
  function object = 'lib/commands/vendor/venue_hours/create', object: context.params.venue_hours, profile: current_profile.user
  assign venue_id = object.properties.venue_id
  
  include 'redirect_to', event_hours: context.params.venue_hours, venue_id: venue_id, notice: 'venue hours created'
  comment

  include 'redirect_to', event_hours: context.params.venue_hours, notice: 'venue hours created'
  endcomment
%}

{{ context.params }}
<br><br>
{{ object }}
<br><br>

