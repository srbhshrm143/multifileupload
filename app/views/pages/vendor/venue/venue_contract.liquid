---
  slug: venue/venue_contract/create
  method: post
---

{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id
  
  function object = 'lib/commands/vendor/venue_contract/create', object: context.params.contract_information, profile: current_profile.user
  include 'redirect_to', venue_contrtact: context.params.contract_information, notice: 'venue advance notice created'
  
  comment
    include 'redirect_to', venue_contrtact: context.params.contract_information, notice: 'venue advance notice created'
  endcomment
%}

{{ context.params }}
<br><br>
{{ object }}
<br><br>

