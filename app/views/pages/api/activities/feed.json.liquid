{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id
  include 'lib/profile_present_or_not_authenticated', profile: current_profile
%}

{% function unread_count = 'lib/queries/activities/unread_count', feed: 'user-private', feed_id: current_profile.id %}


{% render 'theme/json/activities/feed', unread_count: unread_count %}