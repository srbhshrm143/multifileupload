---
slug: api/documents
method: post
---
{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id
  include 'lib/profile_present_or_not_authenticated', profile: current_profile

  function object = 'lib/commands/documents/create', object: context.params.document, current_profile: current_profile

  include 'theme/json/shared/show', object: object
%}
