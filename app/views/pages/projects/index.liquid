{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id, followships: true

  function filters = 'lib/queries/projects/filters', params: context.params
  function projects = 'lib/queries/projects/search', limit: 6, page: filters.page, tag: filters.tag, sort: filters.sort, status: filters.statuses, promoted: false
  function projects_promoted = 'lib/queries/projects/search', limit: 3, page: 1, promoted: true, tag: filters.tag, sort: filters.sort, status: filters.statuses
  function _ = 'lib/extend_context', tags: true, photo_presign: true

  render 'theme/simple/projects/index', projects: projects, projects_promoted: projects_promoted, page: page, filters: filters, current_profile: current_profile, context: context
%}
